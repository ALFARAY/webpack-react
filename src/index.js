import React from "react";
import ReactDOM from "react-dom";
import HolaMundo from "./components/HolaMundo";

ReactDOM.render(<React.StrictMode> <HolaMundo /> </React.StrictMode>, document.getElementById("app"));
